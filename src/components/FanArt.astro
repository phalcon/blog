---
/*
 * The fan art fragment, refreshed from phalcon/assets by the workflow. The
 * committed copy is empty, so a failed download renders nothing rather than
 * stale markup.
 */
import fanart from '../fanart.html?raw';
---

<Fragment set:html={fanart} />
